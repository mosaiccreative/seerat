

## Adjust Chhallan Cover Position

Update the `coverPosition` value for *Chhallan* in `src/data/books.ts` from `"center 65%"` to `"center 55%"` to shift the image crop upward.

### Technical Detail

**File:** `src/data/books.ts` — Change line ~126:
```
coverPosition: "center 65%"  →  coverPosition: "center 55%"
```

